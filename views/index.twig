{% extends 'layout.twig' %}

{% block body %}
  <div class="container mx-auto">
    <h1 class="text-center">{{title}}</h1>
    <p class="text-center">Welcome to {{title}}.</p>
  </div>
{% endblock %}
